# chtemp1
end to end ml pipeline (logging ,exception,etc) , automate the pipeline using dvc (yaml), adding configurable params to pipeline , experiment tracking using dvc live , AWS setup with dvc for data versioning 
